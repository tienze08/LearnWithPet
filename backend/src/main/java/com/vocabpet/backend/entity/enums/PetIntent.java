package com.vocabpet.backend.entity.enums;

/** Why the companion is acting. Animation and dialogue are selected afterwards. */
public enum PetIntent {
    REST,
    MORNING_GREETING,
    RECONNECT,
    USUAL_STUDY_TIME,
    REVISIT_TRICKY_WORD,
    INVITE_STUDY,
    CELEBRATE_PROGRESS,
    AMBIENT_WAITING
}
