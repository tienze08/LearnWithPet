spring:
    datasource:
        url: jdbc:mysql://localhost:3306/vocab_pet
        username: root
        password: 0346443950

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
app:
    gemini:
        api-key: ${GEMINI_API_KEY:}

server:
    port: 8080
